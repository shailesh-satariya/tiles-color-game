const CONFIG: Record<string, number> = {
    rows: 6,
    minRows: 3,
    maxRows: 12,

    columns: 6,
    minColumns: 3,
    maxColumns: 12,

    colorsCnt: 4,
    minColorsCnt: 3,
    maxColorsCnt: 8,
};

export default CONFIG;
